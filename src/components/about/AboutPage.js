import React from "react";

const AboutPage = () => (
  <div>
    <h2>About</h2>
    <p>
      This app uses React, Redux, React Router, and Sagas to build a Pokemon
      App.
    </p>
  </div>
);

export default AboutPage;
